# shellcheck shell=zsh
# Logout commands {{{

# Source shared functions {{{
source ~/.zshared || return
# End Source shared functions }}}

zdebug "Running ~/.zlogout"

# Preserve ssh-agent between shells (removed auto-kill)

# End Logout commands }}}
