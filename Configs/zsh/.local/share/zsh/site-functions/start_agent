# ==============================================================================
# SSH start agent
# ==============================================================================
# Start agent, save configuration and then source it.
SSH_ENV="$HOME/.ssh/agent.env"
/usr/bin/ssh-agent | sed 's/^echo/#echo/' >| "${SSH_ENV}"
chmod 600 "$SSH_ENV"
. "$SSH_ENV" > /dev/null

