# shellcheck shell=zsh
# Profile shell initialization (login shell environment setup)
# Sourced once at login before .zlogin
# Use for environment variables and startup programs

# Currently empty - zsh environment is configured in .zshenv
# Add login-specific environment setup here if needed
