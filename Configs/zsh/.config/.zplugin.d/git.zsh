# This is the configuration file for the zimfw/git module
# See: https://github.com/zimfw/git

# Git module configuration {{{
# Set a custom prefix for the generated aliases. 
# The default prefix is 'G'.
zstyle ':zim:git' aliases-prefix 'G'
# End Git Module configuration }}}
