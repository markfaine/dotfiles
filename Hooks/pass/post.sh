## This script should follow the format of the other scripts.
# It will get the repo from ~/.config/pass/config
# Clone password database project as ~/.password-database if it doesn't already exist.
#
# If the docker-credential-helper is available
# Query pass db for docker-credential-helpers/docker-pass-initialized-check
# If not found
# pass insert docker-credential-helpers/docker-pass-initialized-check
# "pass is initialized"
exit 0
